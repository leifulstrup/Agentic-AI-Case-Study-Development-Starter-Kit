# docs

Presentation assets. Not needed to author a case.

| File | Purpose |
|------|---------|
| `workflow-map.svg` | The clone-to-classroom workflow map as a standalone image — for slides, handouts, or printing. The same diagram renders inline in the main README as Mermaid; that version is authoritative and updates with the workflow. Keep this one in sync when the workflow changes. |
